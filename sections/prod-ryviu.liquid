<section class="Section Section--spacingNormal Section-FBT">
	<div class="Container gridt-wrapper">
		<div class="lt-block-reviews"> 
        <ryviu-widget handle="{{product.handle}}" product_id="{{product.id}}" title_product="{{product.title}}" image_product="{{ product.featured_image.src | img_url: '100x' }}"></ryviu-widget>
    </div>
	</div>
</section>
{% schema %}
{
  "name": "Ryviu",
  "class": "shopify-section--grid-prod-ryviu",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Feature's Title"
    }
  ],
  "presets": [
    {
      "category": "Product",
      "name": "Ryviu",
      "settings": {}
    }
  ]
}
{% endschema %}